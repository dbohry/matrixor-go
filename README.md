### matrixor-server (golang)

#### Add config.json
```
{
  "server": {
    "port": 8080,
    "timeout": 30
  },
  "database": {
    "uri": "mongodb://admin:admin@host/?tls=false",
    "name": "db",
    "collection": "collection"
  }
}
```
